'use strict';

/**
 * defi-strategy-asset service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::defi-strategy-asset.defi-strategy-asset');
