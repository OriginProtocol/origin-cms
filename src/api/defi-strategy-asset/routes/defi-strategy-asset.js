'use strict';

/**
 * defi-strategy-asset router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::defi-strategy-asset.defi-strategy-asset');
